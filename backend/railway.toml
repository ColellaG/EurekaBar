[build]
builder = "NIXPACKS"
buildCommand = "echo 'Building application...'"

[deploy]
startCommand = "python manage.py migrate && python manage.py collectstatic --noinput && gunicorn bar_menu.wsgi"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3 